var Hooks = {
    filterUrl:{
        FilterUrlDefault:require('../_core/plugins/FilterUrl')
    },
    parseBuffer:{
        ParseBufferDefault:require('../_core/plugins/ParseBuffer')
    }
}

module.exports = Hooks;